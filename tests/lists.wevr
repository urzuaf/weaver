# server basic config
# testing blocks and assignments

 serverLists "servers" {
    servers = [
        server "web-prod" {
            host = "192.168.1.100"
            ports = [443, 80, 8080]
            ssl = true
        },
        server "back-prod" {
            host = "192.168.1.101"
            ports = [5432]
            ssl = false
        }
    ]
}

